import SwiftUI

struct LoadingView: View {
    var body: some View {
        Text("Loadign...")
            .font(.largeTitle)
    }
}

struct LoadingView_Previews: PreviewProvider {
    static var previews: some View {
        LoadingView()
    }
}
